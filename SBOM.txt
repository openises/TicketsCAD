TicketsCAD NewUI — Software Bill of Materials
========================================================================

Application:      TicketsCAD NewUI 4.2.19
SBOM author:      OpenISES TicketsCAD Project
SBOM version:     34
SBOM serial:      urn:uuid:69cacc63-3cc5-5086-816c-8ea81af961ae
Generated:        2026-08-14T17:10:33Z
Generated by:     TicketsCAD SBOM generator (tools/generate-sbom.php) 2.1.0
Generation phase: pre-build (from source, before build)
Machine-readable: SBOM.cdx.json (CycloneDX 1.6)
Standard:         CISA 2026 Minimum Elements for a Software Bill of
                  Materials (SBOM), published 2026-07-29
Regenerate with:  php tools/generate-sbom.php

Total components: 95
Components with at least one field unknown to the SBOM author: 95
(Unknown values are stated explicitly below rather than guessed.)

------------------------------------------------------------------------
PHP DEPENDENCIES (COMPOSER, EXACT VERSIONS FROM COMPOSER.LOCK)
------------------------------------------------------------------------
  brick/math                                 0.17.2         MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  cboden/ratchet                             0.4.4          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  evenement/evenement                        3.0.2          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  firebase/php-jwt                           7.0.3          BSD-3-Clause
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  guzzlehttp/guzzle                          7.15.2         MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  guzzlehttp/promises                        2.5.1          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  guzzlehttp/psr7                            2.13.0         MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  minishlink/web-push                        9.0.4          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  psr/clock                                  1.0.0          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  psr/http-client                            1.0.3          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  psr/http-factory                           1.1.0          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  psr/http-message                           2.0            MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  ralouphie/getallheaders                    3.0.3          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  ratchet/pawl                               0.3.5          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  ratchet/rfc6455                            0.3.1          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  react/cache                                1.2.0          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  react/dns                                  1.14.0         MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  react/event-loop                           1.6.0          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  react/promise                              3.3.0          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  react/socket                               1.17.0         MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  react/stream                               1.4.0          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  spomky-labs/base64url                      2.0.4          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  spomky-labs/pki-framework                  1.4.2          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  symfony/deprecation-contracts              3.7.1          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  symfony/http-foundation                    6.4.42         MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  symfony/polyfill-mbstring                  1.38.2         MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  symfony/polyfill-php80                     1.37.0         MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  symfony/polyfill-php82                     1.38.1         MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  symfony/polyfill-php83                     1.38.2         MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  symfony/routing                            6.4.41         MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.
  web-token/jwt-library                      4.1.7          MIT
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm
      why: Package is installed by Composer at deploy time from the
      pinned source commit; the executable artifact is not distributed with
      this repository, so the SBOM author has no artifact to hash. The pinned
      git commit reference is supplied as a Component Identifier instead.

------------------------------------------------------------------------
BROWSER LIBRARIES SHIPPED IN THIS REPOSITORY
------------------------------------------------------------------------
  Bootstrap                                  5.3.3          MIT
      unknown to SBOM author: Component Dependency Relationship
      why: The onward dependencies of this component are not stated: it
      is not described by any lockfile in this repository, so the SBOM author
      has no evidence of what it itself depends on. Its relationship TO this
      application is recorded — the application depends on it.
  Bootstrap Icons                            1.11.3         MIT
      unknown to SBOM author: Component Dependency Relationship
      why: The onward dependencies of this component are not stated: it
      is not described by any lockfile in this repository, so the SBOM author
      has no evidence of what it itself depends on. Its relationship TO this
      application is recorded — the application depends on it.
  GridStack.js                               10.3.1         MIT
      unknown to SBOM author: Component Dependency Relationship
      why: The onward dependencies of this component are not stated: it
      is not described by any lockfile in this repository, so the SBOM author
      has no evidence of what it itself depends on. Its relationship TO this
      application is recorded — the application depends on it.
  Leaflet                                    1.9.4          BSD-2-Clause
      unknown to SBOM author: Component Dependency Relationship
      why: The onward dependencies of this component are not stated: it
      is not described by any lockfile in this repository, so the SBOM author
      has no evidence of what it itself depends on. Its relationship TO this
      application is recorded — the application depends on it.
  leaflet-openweathermap                     version unknown CC0-1.0
      unknown to SBOM author: Component Version, Component Dependency Relationship
      why: The shipped files declare a license (CC0) and a project page
      but carry no version string, and no manifest records the version. The
      onward dependencies of this component are not stated: it is not
      described by any lockfile in this repository, so the SBOM author has no
      evidence of what it itself depends on. Its relationship TO this
      application is recorded — the application depends on it.
  Leaflet.Graticule                          version unknown license unknown
      unknown to SBOM author: Component Version, Component License, Component Dependency Relationship
      why: The shipped file has a three-line header with no version and
      no license declaration, and no manifest records it. The upstream project
      is MIT-licensed, but that is not evidenced by the artifact actually
      shipped, so the SBOM author does not assert it. The onward dependencies
      of this component are not stated: it is not described by any lockfile in
      this repository, so the SBOM author has no evidence of what it itself
      depends on. Its relationship TO this application is recorded — the
      application depends on it.
  marked                                     12.0.2         MIT
      unknown to SBOM author: Component Dependency Relationship
      why: The onward dependencies of this component are not stated: it
      is not described by any lockfile in this repository, so the SBOM author
      has no evidence of what it itself depends on. Its relationship TO this
      application is recorded — the application depends on it.
  qrcode-generator                           1.4.4          MIT
      unknown to SBOM author: Component Dependency Relationship
      why: The onward dependencies of this component are not stated: it
      is not described by any lockfile in this repository, so the SBOM author
      has no evidence of what it itself depends on. Its relationship TO this
      application is recorded — the application depends on it.
  qrcode-generator (unminified copy)         version unknown MIT
      unknown to SBOM author: Component Version, Component Dependency Relationship
      why: This second, unminified copy of Kazuhiko Arase's QR generator
      carries a copyright header but no version string anywhere in the file,
      and it is not tracked by any package manifest. The SBOM author cannot
      determine which upstream release it corresponds to. It is retained as a
      distinct component because it is a separate shipped artifact loaded by
      profile.php, at a possibly different version from the copy above. The
      onward dependencies of this component are not stated: it is not
      described by any lockfile in this repository, so the SBOM author has no
      evidence of what it itself depends on. Its relationship TO this
      application is recorded — the application depends on it.

------------------------------------------------------------------------
OPTIONAL PYTHON SERVICE DEPENDENCIES (INSTALLED BY THE OPERATOR)
------------------------------------------------------------------------
  aprslib                                    version unknown Apache-2.0
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Optional Python service dependency, installed with pip on the
      operator's host at deploy time and not distributed with this software.
      The producer is whoever controls this name on PyPI at install time,
      which the SBOM author cannot pin from this tree. No requirements file
      pins this package; it is installed by services/aprs/install.sh with no
      version constraint written, so pip resolves whatever is current on PyPI
      at the moment the operator runs the installer. Stating a version here
      would be a guess, and a guessed version is a false attestation. The
      resolved version is therefore known only on the operator's host;
      operators should generate a deployment SBOM (for example with "pip
      freeze") to capture it. The onward dependencies of this component are
      not stated: it is not described by any lockfile in this repository, so
      the SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.
  esptool                                    version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component License, Component Dependency Relationship
      why: Optional Python service dependency, installed with pip on the
      operator's host at deploy time and not distributed with this software.
      The producer is whoever controls this name on PyPI at install time,
      which the SBOM author cannot pin from this tree. No requirements file
      pins this package and no installer in this repository names a version
      for it. The resolved version is therefore known only on the operator's
      host; operators should generate a deployment SBOM (for example with "pip
      freeze") to capture it. The onward dependencies of this component are
      not stated: it is not described by any lockfile in this repository, so
      the SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.
  faster-whisper                             version unknown MIT
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Optional Python service dependency, installed with pip on the
      operator's host at deploy time and not distributed with this software.
      The producer is whoever controls this name on PyPI at install time,
      which the SBOM author cannot pin from this tree. No requirements file
      pins this package; it is installed by
      services/dvswitch/install-bridge.sh with no version constraint written,
      so pip resolves whatever is current on PyPI at the moment the operator
      runs the installer. Stating a version here would be a guess, and a
      guessed version is a false attestation. The resolved version is
      therefore known only on the operator's host; operators should generate a
      deployment SBOM (for example with "pip freeze") to capture it. The
      onward dependencies of this component are not stated: it is not
      described by any lockfile in this repository, so the SBOM author has no
      evidence of what it itself depends on. Its relationship TO this
      application is recorded — the application depends on it.
  meshcore                                   version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component License, Component Dependency Relationship
      why: Optional Python service dependency, installed with pip on the
      operator's host at deploy time and not distributed with this software.
      The producer is whoever controls this name on PyPI at install time,
      which the SBOM author cannot pin from this tree. No requirements file
      pins this package and no installer in this repository names a version
      for it. The resolved version is therefore known only on the operator's
      host; operators should generate a deployment SBOM (for example with "pip
      freeze") to capture it. The onward dependencies of this component are
      not stated: it is not described by any lockfile in this repository, so
      the SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.
  meshcore-cli                               version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component License, Component Dependency Relationship
      why: Optional Python service dependency, installed with pip on the
      operator's host at deploy time and not distributed with this software.
      The producer is whoever controls this name on PyPI at install time,
      which the SBOM author cannot pin from this tree. No requirements file
      pins this package and no installer in this repository names a version
      for it. The resolved version is therefore known only on the operator's
      host; operators should generate a deployment SBOM (for example with "pip
      freeze") to capture it. The onward dependencies of this component are
      not stated: it is not described by any lockfile in this repository, so
      the SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.
  meshtastic                                 version unknown GPL-3.0-only
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Optional Python service dependency, installed with pip on the
      operator's host at deploy time and not distributed with this software.
      The producer is whoever controls this name on PyPI at install time,
      which the SBOM author cannot pin from this tree. The declared constraint
      is ">=2.7.11", which bounds but does not determine the installed
      version. The resolved version is therefore known only on the operator's
      host; operators should generate a deployment SBOM (for example with "pip
      freeze") to capture it. The onward dependencies of this component are
      not stated: it is not described by any lockfile in this repository, so
      the SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.
  mysql-connector-python                     version unknown GPL-2.0-only WITH Universal-FOSS-exception-1.0
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Optional Python service dependency, installed with pip on the
      operator's host at deploy time and not distributed with this software.
      The producer is whoever controls this name on PyPI at install time,
      which the SBOM author cannot pin from this tree. No requirements file
      pins this package; it is installed by services/aprs/install.sh with no
      version constraint written, so pip resolves whatever is current on PyPI
      at the moment the operator runs the installer. Stating a version here
      would be a guess, and a guessed version is a false attestation. The
      resolved version is therefore known only on the operator's host;
      operators should generate a deployment SBOM (for example with "pip
      freeze") to capture it. The onward dependencies of this component are
      not stated: it is not described by any lockfile in this repository, so
      the SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.
  numpy                                      version unknown BSD-3-Clause
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Optional Python service dependency, installed with pip on the
      operator's host at deploy time and not distributed with this software.
      The producer is whoever controls this name on PyPI at install time,
      which the SBOM author cannot pin from this tree. No requirements file
      pins this package; it is installed by
      services/dvswitch/install-bridge.sh with no version constraint written,
      so pip resolves whatever is current on PyPI at the moment the operator
      runs the installer. Stating a version here would be a guess, and a
      guessed version is a false attestation. The resolved version is
      therefore known only on the operator's host; operators should generate a
      deployment SBOM (for example with "pip freeze") to capture it. The
      onward dependencies of this component are not stated: it is not
      described by any lockfile in this repository, so the SBOM author has no
      evidence of what it itself depends on. Its relationship TO this
      application is recorded — the application depends on it.
  onnxruntime                                version unknown MIT
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Optional Python service dependency, installed with pip on the
      operator's host at deploy time and not distributed with this software.
      The producer is whoever controls this name on PyPI at install time,
      which the SBOM author cannot pin from this tree. No requirements file
      pins this package; it is installed by
      services/dvswitch/install-bridge.sh with no version constraint written,
      so pip resolves whatever is current on PyPI at the moment the operator
      runs the installer. Stating a version here would be a guess, and a
      guessed version is a false attestation. The resolved version is
      therefore known only on the operator's host; operators should generate a
      deployment SBOM (for example with "pip freeze") to capture it. The
      onward dependencies of this component are not stated: it is not
      described by any lockfile in this repository, so the SBOM author has no
      evidence of what it itself depends on. Its relationship TO this
      application is recorded — the application depends on it.
  paho-mqtt                                  version unknown EPL-2.0
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Optional Python service dependency, installed with pip on the
      operator's host at deploy time and not distributed with this software.
      The producer is whoever controls this name on PyPI at install time,
      which the SBOM author cannot pin from this tree. The declared constraint
      is ">=2.1.0", which bounds but does not determine the installed version.
      The resolved version is therefore known only on the operator's host;
      operators should generate a deployment SBOM (for example with "pip
      freeze") to capture it. The onward dependencies of this component are
      not stated: it is not described by any lockfile in this repository, so
      the SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.
  pip                                        version unknown MIT
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Optional Python service dependency, installed with pip on the
      operator's host at deploy time and not distributed with this software.
      The producer is whoever controls this name on PyPI at install time,
      which the SBOM author cannot pin from this tree. No requirements file
      pins this package; it is installed by
      services/dvswitch/install-bridge.sh with no version constraint written,
      so pip resolves whatever is current on PyPI at the moment the operator
      runs the installer. Stating a version here would be a guess, and a
      guessed version is a false attestation. The resolved version is
      therefore known only on the operator's host; operators should generate a
      deployment SBOM (for example with "pip freeze") to capture it. The
      onward dependencies of this component are not stated: it is not
      described by any lockfile in this repository, so the SBOM author has no
      evidence of what it itself depends on. Its relationship TO this
      application is recorded — the application depends on it.
  piper-tts                                  version unknown MIT
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Optional Python service dependency, installed with pip on the
      operator's host at deploy time and not distributed with this software.
      The producer is whoever controls this name on PyPI at install time,
      which the SBOM author cannot pin from this tree. No requirements file
      pins this package; it is installed by
      services/dvswitch/install-bridge.sh with no version constraint written,
      so pip resolves whatever is current on PyPI at the moment the operator
      runs the installer. Stating a version here would be a guess, and a
      guessed version is a false attestation. The resolved version is
      therefore known only on the operator's host; operators should generate a
      deployment SBOM (for example with "pip freeze") to capture it. The
      onward dependencies of this component are not stated: it is not
      described by any lockfile in this repository, so the SBOM author has no
      evidence of what it itself depends on. Its relationship TO this
      application is recorded — the application depends on it.
  pypubsub                                   version unknown BSD-2-Clause
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Optional Python service dependency, installed with pip on the
      operator's host at deploy time and not distributed with this software.
      The producer is whoever controls this name on PyPI at install time,
      which the SBOM author cannot pin from this tree. No requirements file
      pins this package; it is installed by assets/js/mesh-console.js with no
      version constraint written, so pip resolves whatever is current on PyPI
      at the moment the operator runs the installer. Stating a version here
      would be a guess, and a guessed version is a false attestation. The
      resolved version is therefore known only on the operator's host;
      operators should generate a deployment SBOM (for example with "pip
      freeze") to capture it. The onward dependencies of this component are
      not stated: it is not described by any lockfile in this repository, so
      the SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.
  pyserial                                   version unknown BSD-3-Clause
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Optional Python service dependency, installed with pip on the
      operator's host at deploy time and not distributed with this software.
      The producer is whoever controls this name on PyPI at install time,
      which the SBOM author cannot pin from this tree. No requirements file
      pins this package; it is installed by assets/js/mesh-console.js with no
      version constraint written, so pip resolves whatever is current on PyPI
      at the moment the operator runs the installer. Stating a version here
      would be a guess, and a guessed version is a false attestation. The
      resolved version is therefore known only on the operator's host;
      operators should generate a deployment SBOM (for example with "pip
      freeze") to capture it. The onward dependencies of this component are
      not stated: it is not described by any lockfile in this repository, so
      the SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.
  requests                                   version unknown Apache-2.0
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Optional Python service dependency, installed with pip on the
      operator's host at deploy time and not distributed with this software.
      The producer is whoever controls this name on PyPI at install time,
      which the SBOM author cannot pin from this tree. The declared constraint
      is ">=2.34.2", which bounds but does not determine the installed
      version. The resolved version is therefore known only on the operator's
      host; operators should generate a deployment SBOM (for example with "pip
      freeze") to capture it. The onward dependencies of this component are
      not stated: it is not described by any lockfile in this repository, so
      the SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.
  vosk                                       version unknown Apache-2.0
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Optional Python service dependency, installed with pip on the
      operator's host at deploy time and not distributed with this software.
      The producer is whoever controls this name on PyPI at install time,
      which the SBOM author cannot pin from this tree. No requirements file
      pins this package and no installer in this repository names a version
      for it. The resolved version is therefore known only on the operator's
      host; operators should generate a deployment SBOM (for example with "pip
      freeze") to capture it. The onward dependencies of this component are
      not stated: it is not described by any lockfile in this repository, so
      the SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.

------------------------------------------------------------------------
DERIVED SOURCE (THIRD-PARTY CODE PORTED INTO THIS TREE)
------------------------------------------------------------------------
  MMDVMHost                                  version unknown GPL-2.0-only
      unknown to SBOM author: Component Version, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: The ported files cite MMDVMHost source filenames but no
      upstream release tag or commit, so the SBOM author cannot state which
      MMDVMHost revision the port was taken from. There is also no single
      artifact to hash: this component is source ported into several files
      rather than a distributed package. A SHA-256 for each individual ported
      file is given in the "ticketscad:file" properties on this component,
      which is the integrity evidence a recipient can actually check against
      this tree. The onward dependencies of this component are not stated: it
      is not described by any lockfile in this repository, so the SBOM author
      has no evidence of what it itself depends on. Its relationship TO this
      application is recorded — the application depends on it.

------------------------------------------------------------------------
SPECIFICATION DATA SHIPPED IN THIS REPOSITORY
------------------------------------------------------------------------
  CycloneDX specification schemas            1.6            Apache-2.0
      unknown to SBOM author: Component Dependency Relationship
      why: Specification data with no onward dependencies of its own to
      declare. Its relationship TO this application is recorded — the
      application ships it. The onward dependencies of this component are not
      stated: it is not described by any lockfile in this repository, so the
      SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.

------------------------------------------------------------------------
CONTAINER BASE IMAGES (OPTIONAL DOCKER DEPLOYMENT)
------------------------------------------------------------------------
  composer                                   2              license unknown
      unknown to SBOM author: Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: The image is referenced by a mutable tag rather than a
      digest, so the exact image contents are resolved at "docker build" time
      on the operator's host and cannot be hashed by the SBOM author. For the
      same reason the producer and licence cannot be stated: a base image is a
      whole distribution of many packages under many licences, and which ones
      it contains depends on when the tag is pulled. Operators who need this
      should generate an image SBOM from the built image (for example with
      "syft" or "trivy"). The onward dependencies of this component are not
      stated: it is not described by any lockfile in this repository, so the
      SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.
  debian                                     bookworm-slim  license unknown
      unknown to SBOM author: Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: The image is referenced by a mutable tag rather than a
      digest, so the exact image contents are resolved at "docker build" time
      on the operator's host and cannot be hashed by the SBOM author. For the
      same reason the producer and licence cannot be stated: a base image is a
      whole distribution of many packages under many licences, and which ones
      it contains depends on when the tag is pulled. Operators who need this
      should generate an image SBOM from the built image (for example with
      "syft" or "trivy"). The onward dependencies of this component are not
      stated: it is not described by any lockfile in this repository, so the
      SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.
  mariadb                                    11             license unknown
      unknown to SBOM author: Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: The image is referenced by a mutable tag rather than a
      digest, so the exact image contents are resolved at "docker build" time
      on the operator's host and cannot be hashed by the SBOM author. For the
      same reason the producer and licence cannot be stated: a base image is a
      whole distribution of many packages under many licences, and which ones
      it contains depends on when the tag is pulled. Operators who need this
      should generate an image SBOM from the built image (for example with
      "syft" or "trivy"). The onward dependencies of this component are not
      stated: it is not described by any lockfile in this repository, so the
      SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.
  php                                        8.2-apache-bookworm license unknown
      unknown to SBOM author: Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: The image is referenced by a mutable tag rather than a
      digest, so the exact image contents are resolved at "docker build" time
      on the operator's host and cannot be hashed by the SBOM author. For the
      same reason the producer and licence cannot be stated: a base image is a
      whole distribution of many packages under many licences, and which ones
      it contains depends on when the tag is pulled. Operators who need this
      should generate an image SBOM from the built image (for example with
      "syft" or "trivy"). The onward dependencies of this component are not
      stated: it is not described by any lockfile in this repository, so the
      SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.

------------------------------------------------------------------------
OPERATING-SYSTEM PACKAGES INSTALLED BY OUR DOCKERFILES AND INSTALLERS
------------------------------------------------------------------------
  ca-certificates                            version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  curl                                       version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  default-mysql-client                       version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  ffmpeg                                     version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  gcc-arm-linux-gnueabi                      version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  git                                        version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  gnupg                                      version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  libcurl4-openssl-dev                       version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  libfreetype6-dev                           version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  libjpeg62-turbo-dev                        version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  libonig-dev                                version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  libpng-dev                                 version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  libssl-dev                                 version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  libxml2-dev                                version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  libzip-dev                                 version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  make                                       version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  python3                                    version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  python3-numpy                              version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  python3-pip                                version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  python3-venv                               version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  qemu-user-static                           version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  unzip                                      version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.
  zlib1g-dev                                 version unknown license unknown
      unknown to SBOM author: Component Version, Component Producer, Component License, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Installed with apt at image-build or install time and not
      distributed with this software. No version is written in the installing
      file, so apt resolves whatever the host's configured archive currently
      serves; the SBOM author cannot state a version, a producer, a licence or
      a hash from this tree, because all four depend on which distribution and
      which archive snapshot the operator builds against. Operators who need
      these should generate an SBOM from the built image (for example with
      "syft" or "trivy"), which reads the package database and resolves all of
      them exactly. The onward dependencies of this component are not stated:
      it is not described by any lockfile in this repository, so the SBOM
      author has no evidence of what it itself depends on. Its relationship TO
      this application is recorded — the application depends on it.

------------------------------------------------------------------------
ARTIFACTS DOWNLOADED AT INSTALL TIME OR FIRST USE (BINARIES, MODELS, REFERENCE DATA)
------------------------------------------------------------------------
  faster-whisper model weights               version unknown license unknown
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm, Component Producer, Component License, Component Version, Component Dependency Relationship
      why: Not fetched by this project at all: the faster-whisper
      library downloads the weights into the operator's Hugging Face cache the
      first time transcription runs. Which repository and revision that
      resolves to is decided by the library at runtime, so no version,
      producer, licence or hash can be stated from this tree. The model size
      is configurable, so the identifier above records the default the code
      requests, not a guarantee of what an operator has. The onward
      dependencies of this component are not stated: it is not described by
      any lockfile in this repository, so the SBOM author has no evidence of
      what it itself depends on. Its relationship TO this application is
      recorded — the application depends on it.
  FCC ULS licence database (amateur and GMRS) version unknown license unknown
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm, Component License, Component Version, Component Dependency Relationship
      why: Optional reference data for callsign lookups, downloaded by
      an operator-run tool. The FCC publishes it as a rolling dump at a fixed
      URL with no version identifier and no published checksum, so the only
      thing that distinguishes one copy from another is when it was fetched.
      It is US Government work and not subject to copyright, which is not the
      same as carrying an SPDX licence identifier, so none is asserted. The
      onward dependencies of this component are not stated: it is not
      described by any lockfile in this repository, so the SBOM author has no
      evidence of what it itself depends on. Its relationship TO this
      application is recorded — the application depends on it.
  GeoNames postal-code dataset (US)          version unknown license unknown
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm, Component License, Component Version, Component Dependency Relationship
      why: Optional reference data, downloaded by an operator-run tool
      from a fixed URL that always serves the current export. No version
      identifier and no published checksum accompany it. GeoNames publishes
      under a Creative Commons licence, but the exact version of that licence
      is not recorded in this tree and is not asserted from memory. The onward
      dependencies of this component are not stated: it is not described by
      any lockfile in this repository, so the SBOM author has no evidence of
      what it itself depends on. Its relationship TO this application is
      recorded — the application depends on it.
  md380-emu (AMBE vocoder emulator)          version unknown license unknown
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm, Component Producer, Component License, Component Version, Component Dependency Relationship
      why: Obtained by one of four routes depending on how the operator
      installs — copied from a peer host, built from a "--depth 1" clone of
      the md380tools default branch, downloaded from an operator-supplied
      MD380_URL, or extracted from a .deb matched by a glob in the DVSwitch
      repository. None of the four pins a version, so no version, hash or
      producer can be stated here; the licence of the resulting binary depends
      on which route was taken. This is the least reproducible component in
      the system and is recorded as such rather than omitted. The onward
      dependencies of this component are not stated: it is not described by
      any lockfile in this repository, so the SBOM author has no evidence of
      what it itself depends on. Its relationship TO this application is
      recorded — the application depends on it.
  Piper voice model (en_US-lessac-medium)    version unknown license unknown
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm, Component License, Component Version, Component Dependency Relationship
      why: Fetched from the "main" branch of a Hugging Face repository,
      which is a moving reference rather than a release: the bytes served
      under that path can change without any identifier in this tree changing.
      There is no version to state and no published hash to record. The voice
      is selectable with PIPER_VOICE, so an operator may hold a different
      model entirely. The onward dependencies of this component are not
      stated: it is not described by any lockfile in this repository, so the
      SBOM author has no evidence of what it itself depends on. Its
      relationship TO this application is recorded — the application depends
      on it.
  Vosk speech-recognition model (vosk-model-small-en-us-0.15) 0.15           license unknown
      unknown to SBOM author: Component Hash Value, Component Hash Algorithm, Component License, Component Dependency Relationship
      why: Optional: speech-to-text is off unless the operator sets
      DMR_VOSK_MODEL. The model version 0.15 appears in the archive filename
      and is recorded above on that evidence, but the archive carries no
      published checksum for the SBOM author to record, and the operator
      chooses which model to install. The licence of the model weights is not
      stated in this tree and is not asserted here. The onward dependencies of
      this component are not stated: it is not described by any lockfile in
      this repository, so the SBOM author has no evidence of what it itself
      depends on. Its relationship TO this application is recorded — the
      application depends on it.

------------------------------------------------------------------------
BUILD- AND VERIFICATION-TIME TOOLING FETCHED FROM A REGISTRY
------------------------------------------------------------------------
  actions/checkout                           version unknown MIT
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Referenced by the major-version constraint "v7", which bounds
      but does not determine what is fetched: the registry resolves it to
      whatever the newest matching release is at the moment the job runs. The
      SBOM author therefore cannot state the resolved version, its publisher
      at that moment, or a hash of the fetched bytes. This is a real
      supply-chain exposure in the build path and is recorded here rather than
      left out because it is only build tooling. The onward dependencies of
      this component are not stated: it is not described by any lockfile in
      this repository, so the SBOM author has no evidence of what it itself
      depends on. Its relationship TO this application is recorded — the
      application depends on it.
  ajv-cli                                    version unknown MIT
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Referenced by the major-version constraint "5", which bounds
      but does not determine what is fetched: the registry resolves it to
      whatever the newest matching release is at the moment the job runs. The
      SBOM author therefore cannot state the resolved version, its publisher
      at that moment, or a hash of the fetched bytes. This is a real
      supply-chain exposure in the build path and is recorded here rather than
      left out because it is only build tooling. The onward dependencies of
      this component are not stated: it is not described by any lockfile in
      this repository, so the SBOM author has no evidence of what it itself
      depends on. Its relationship TO this application is recorded — the
      application depends on it.
  ajv-formats                                version unknown MIT
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Referenced by the major-version constraint "3", which bounds
      but does not determine what is fetched: the registry resolves it to
      whatever the newest matching release is at the moment the job runs. The
      SBOM author therefore cannot state the resolved version, its publisher
      at that moment, or a hash of the fetched bytes. This is a real
      supply-chain exposure in the build path and is recorded here rather than
      left out because it is only build tooling. The onward dependencies of
      this component are not stated: it is not described by any lockfile in
      this repository, so the SBOM author has no evidence of what it itself
      depends on. Its relationship TO this application is recorded — the
      application depends on it.
  shivammathur/setup-php                     version unknown MIT
      unknown to SBOM author: Component Version, Component Producer, Component Hash Value, Component Hash Algorithm, Component Dependency Relationship
      why: Referenced by the major-version constraint "v2", which bounds
      but does not determine what is fetched: the registry resolves it to
      whatever the newest matching release is at the moment the job runs. The
      SBOM author therefore cannot state the resolved version, its publisher
      at that moment, or a hash of the fetched bytes. This is a real
      supply-chain exposure in the build path and is recorded here rather than
      left out because it is only build tooling. The onward dependencies of
      this component are not stated: it is not described by any lockfile in
      this repository, so the SBOM author has no evidence of what it itself
      depends on. Its relationship TO this application is recorded — the
      application depends on it.

========================================================================
Reporting a problem with this SBOM, or with any component listed in it:
see SECURITY.md in this repository.
