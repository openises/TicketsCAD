# Soft-delete audit exceptions — tools/soft_delete_audit.php
#
# That gate finds every SELECT that reads `ticket` (directly, or via a
# resolvable $where-style variable) without a `deleted_at` exclusion term
# anywhere it can find one, and fails the suite on anything not listed here.
#
# FORMAT   path:line | reason        (both required; a reason-less entry
#                                     fails the gate, same as
#                                     tools/doc_nav_label_exceptions.txt)
#
# Adding a line here is a claim that the finding is correct as written —
# either the read is a legitimate case that must still see deleted rows
# (Eric's own example: an incident-number collision check must see a
# deleted incident's number to avoid reissuing it), or the tool's resolver
# genuinely cannot see a filter that is really there (a pre-wastebasket
# compatibility fallback that intentionally omits `deleted_at`; a downstream
# query that only ever executes after an earlier query in the same request
# has already excluded deleted rows). It is NOT the place to park a real
# gap that's inconvenient to fix — fix those.
#
# Every line must still match a real finding, or the gate treats it as
# STALE and fails — same discipline as the nav-label gate: a fixed site (or
# code that moved) makes its exception line meaningless, and a meaningless
# line left on file is how the next real regression hides in plain sight.
#
# ── CATEGORY A: legitimately needs to see deleted rows ──────────────────────
# Audit/history views and reference-integrity checks that operate ON
# deleted records (or must see them to stay consistent), not around them.

api/log.php:62 | audit log viewer — a "Incident Deleted" log entry (code 15) is ABOUT the very ticket it references; the audit trail must keep resolving its scope/case-number even after deletion, not blank out the one entry that explains what happened.
api/audit-log.php:260 | audit log viewer — batched incident_number lookup for display; same reasoning as api/log.php:62, and it must resolve numbers for entries about since-deleted incidents too. (line shifted by the GH#37 export action added above it, 2026-08-08 — same code, same reasoning.)
api/message-tray.php:79 | _mt_logged_ticket() shows which incident a message was historically attached to (informational record of a past action), not a live incident browser; matches the audit-log reasoning.
api/time-entries.php:339 | a logged time entry's hours remain valid after its incident is deleted; the entry should keep showing the incident label it was worked under, same as an audit trail.
inc/ics214_timeline.php:63 | personal ICS-214 activity timeline — an accountability record of what a responder did ("dispatched to X"), which must stay intact even if the incident is later deleted; the responder's own signable record should not go blank.
inc/incident-number.php:529 | incnum_find_existing() — Eric's own named example on issue #25: the incident-number collision check must see deleted incidents' numbers too, or the allocator could reissue a number already used by a soft-deleted record.
inc/incident-number.php:448 | incnum_display() is the shared case-number resolver used BY the audit/history views above (api/log.php, api/audit-log.php, inc/par.php's overdue broadcast) — filtering here would make those legitimate uses degrade to the raw numeric id for a deleted incident's entries.
inc/security-labels.php:253 | seclabel_delete()'s reference-integrity check ("is this label still in use") must count ALL referencing tickets, deleted or not, or deleting a label could leave a soft-deleted (and later restorable) incident with a dangling security_label_override_id — same reasoning as the incident-number collision check.
inc/org-scope.php:311 | org_can_see_ticket() is an org-membership permission check (returns a boolean), not a content-serving read; every caller that displays incident data already gates on deletion state separately (see api/incident-detail.php, api/incident-update.php, etc.).
inc/db-health-gate.php:62 | installation health probe ("can we read a core table at all") — existence of any single row, deleted or not, is what it's testing; not user-facing data.
inc/security-labels.php:113 | seclabel_resolve() returns security-label CONFIGURATION (badge colors, EOC display flags) for a ticket id the caller already resolved and gated separately; it does not itself serve incident content, and every caller reaches it only after its own incident-detail-style query has already excluded deleted rows.
inc/par.php:113 | par_resolve_cadence()'s incident-type lookup returns PAR cadence config (minutes/windows), not incident content; reached only with ticket ids already filtered by the PAR roster queries (api/par.php's overdue endpoint, inc/par.php's scheduler) which now exclude deleted_at.
inc/par.php:229 | par_unit_last_activity_at()'s fallback timestamp lookup — internal PAR timing arithmetic, not incident content; same reasoning as inc/par.php:113.
inc/par.php:237 | same function, second fallback timestamp lookup (incident creation time) — internal PAR timing arithmetic only.
inc/par.php:371 | par_unit_last_activity_at()'s final fallback (incident creation time when no other activity signal exists) — internal PAR timing arithmetic only.
api/par.php:200 | api/par.php?action=for_ticket's override-value lookup returns only the numeric cadence override for a ticket id the CALLER already supplied; no incident content is exposed.

# ── CATEGORY B: pre-wastebasket compatibility fallbacks ─────────────────────
# Each of these sits in a catch{} block that intentionally omits
# `deleted_at` because it exists to serve installs that don't have the
# column yet (pre-wastebasket schema). The PRIMARY query right above it
# (same function, same file) already has the term; only the fallback the
# resolver can't see through is listed here.

api/active-event.php:131 | pre-wastebasket compat fallback — the primary query (api/active-event.php, ~line 115-120) already filters `deleted_at IS NULL`; this is the catch{} retry for installs without the column.
api/message-tray.php:203 | pre-wastebasket compat fallback for the ?action=incidents picker — the primary query (~line 182-186) already has the full `deleted_at IS NULL OR = '0000-00-00 00:00:00'` term (this is literally the GH #24 fix); this is the catch{} retry.
api/net-checkins.php:140 | pre-wastebasket compat fallback — the primary query (~line 125-133) already filters `deleted_at IS NULL`; this is the catch{} retry for installs without the column.
inc/auto_close.php:196 | auto_close_maybe_cancel() only CANCELS a pending auto-close (no incident content served, and cancelling is harmless regardless of deletion state); the column-existence question doesn't apply here the way it does to the true schema-fallback entries above, but the reasoning is analogous — this is a no-op-safe path, not a leak. Line moved from 173 (GH#65, 2026-08-15: auto_close_clear_on_close() added earlier in the file); same read, same guard.
inc/par.php:980 | pre-wastebasket compat fallback inside par_run_scheduler() — the primary query (~line 967-971) already has the full `deleted_at IS NULL OR = '0000-00-00 00:00:00'` term; this is the catch{} retry for installs without the column.
net-control.php:62 | pre-wastebasket compat fallback — the primary query (~line 52-58) already filters `(deleted_at IS NULL)`; this is the catch{} retry for installs without the column.

# ── CATEGORY C: verified fixed by hand, unresolvable to this tool ───────────
# Every one of these DOES exclude soft-deleted incidents — the term is
# seeded into a `$where`/`$where_parts` array or variable — but the same
# statement (or an earlier one building the same variable) also folds in
# the return value of org_query_filter() (org-scope filtering), which is a
# function call this tool's resolver cannot evaluate (documented in the
# tool's own "WHAT IT CANNOT SEE" section: any construct beyond string
# literals / heredocs / db_table() / implode() taints the whole variable).
# Verified by reading the source, not by running the app — a fresh eye
# should still double-check origin diffs if these lines move.

api/external/v1/incidents.php:123 | $where is seeded with the deleted_at term as its first element (line ~96) before org_query_filter()'s return value is appended; already fixed in 1502157. Line moved from 112 (2026-08-15, GH RBAC fix: action.view_incident/action.view_incidents replaced with the real incidents.view code, shifting the GET gate's comment block down 11 lines).
api/incident-list.php:101 | $where is seeded with the deleted_at term as its first element before org_query_filter()'s return is appended and $whereClause is built via a ternary + implode() this tool can't evaluate.
api/incident-list.php:114 | same $whereClause as :101 — the count and the row-fetch queries share it.
api/incident-search.php:127 | same shape as api/incident-list.php:101 — $where seeded first, tainted downstream by org_query_filter() + a ternary/implode() whereClause.
api/incident-search.php:136 | same $whereClause as :127 — the count and the row-fetch queries share it.
api/reports.php:322 | 'unit_log' report — $where_parts is seeded with the deleted_at term, but `$where .= $rptTicketFrag;` (org-scope, from a destructuring `[$rptTicketFrag, $rptTicketVars] = org_query_filter(...)` this tool doesn't track) taints the final variable. Line moved from 295 (GH#57 follow-up, 2026-08-15: member_id parsing/IDOR-check/$memberIdFrag setup added ahead of this case, shifting every later case down by 27 lines); same read, same guard.
api/reports.php:401 | 'dispatch_log' report — same shape as api/reports.php:322. Line moved from 374, same reason.
api/reports.php:605 | 'incident_report' report — same shape as api/reports.php:322. Line moved from 578, same reason.
api/reports.php:678 | 'facility_log' report — same shape as api/reports.php:322. Line moved from 651, same reason.

# ── CATEGORY D: downstream of an already-gated query in the same request ────
# Each of these executes only after an earlier query in the SAME function
# has already excluded soft-deleted incidents (typically a 404 on the
# primary incident lookup) — by the time execution reaches this statement,
# the id in scope is already known non-deleted.

api/incident-detail.php:196 | the assignments join executes only after the main incident query above it (api/incident-detail.php:52-79, now filtered) has already returned a non-deleted incident or 404'd. Line moved from 157 by Phase 132 Step 4 (GH #16)'s disposition_id/disposition_label lookup inserted earlier in the same file; same read, same guard.
api/incident-detail.php:112 | Phase 132 Step 4 (GH #16) — the current disposition_id + resolved label lookup, added right after the main incident query's `if (!$incident) { ... 404 ... }` gate (api/incident-detail.php:52-94, now filtered). By the time execution reaches this statement the ticket id is already confirmed non-deleted, same as the assignments join below it.
api/incident-update.php:345 | the severity re-read (for the SSE payload) executes only after the existence guard at the top of this file (api/incident-update.php:69-83, now filtered) has already confirmed the ticket is not deleted. Line moved from 310 by Phase 132 Step 2 (GH #16)'s disposition code inserted earlier in the same action; same read, same guard.
inc/incident-write.php:847 | incident_set_disposition_internal()'s best-effort "prior value, for the audit details" lookup — every current caller (api/incident-update.php's update_status and set_disposition actions, both gated by the existence guard at api/incident-update.php:69-83; and incident_update_status_internal() itself, see the next entry) reaches this function only with an already-confirmed non-deleted ticket id. A failed lookup here (soft-deleted or not) only degrades the audit-log's old_disposition_id detail to null — it never serves content. Line moved from 805 by the 2026-08-12 creation-time security-label block inserted earlier in the same file; same read, same guard.
inc/incident-write.php:998 | incident_update_status_internal()'s disposition_required_on_close enforcement-gate read. Reached from api/incident-update.php's update_status action (gated by the existence guard at api/incident-update.php:69-83) or from inc/auto_close.php's auto_close_sweep(), whose own candidate-ticket query (inc/auto_close.php:222) already excludes deleted_at rows before this function is ever called on that id. Line moved from 956, same reason.
inc/disposition-admin.php:381 | disposition_options_for_ticket_internal()'s (Phase 132 Step 4, GH #16) type-discipline/org/current-value lookup. Its one caller, api/dispositions-picker.php, has its own explicit existence-and-not-deleted guard (mirroring api/incident-update.php's) before ever calling this function, so the ticket id in scope here is already confirmed non-deleted. tests/test_phase132_incident_detail.php calls the function directly (bypassing that endpoint) but only ever against tickets it just created itself in the same run, never a deleted one.
inc/quick-notes.php:117 | pre-wastebasket compat fallback -- the primary query (~line 111-115) already filters `deleted_at IS NULL`; this is the catch{} retry for installs without the column, matching api/net-checkins.php:140's own established pattern.
